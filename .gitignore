# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# the search index is rebuilt from content/ at build time (runs on Vercel too)
/public/search-index.json
# NOTE: content/ IS committed on purpose — it is the published snapshot that
# Vercel builds from (the Obsidian vault never leaves your machine).

# misc
.DS_Store
*.pem
npm-debug.log*
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
